name: houseprice_retrain

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "housing_preprocessed.csv"}
    command: "conda run -n mlflow-env python modelling.py --data_path {data_path}"
